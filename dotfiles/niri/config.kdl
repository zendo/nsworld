include "custom.kdl"

// Overrides
binds {
    Mod+X { spawn-sh "noctalia-shell ipc call launcher toggle"; }
    Mod+V { spawn-sh "noctalia-shell ipc call launcher clipboard"; }
    Mod+Escape { spawn-sh "noctalia-shell ipc call sessionMenu toggle"; }
}
